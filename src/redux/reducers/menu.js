const initialState={
    items:[
        "EATORAMA",
        "SOUP STATION",
        "STARBUCKS"
    ]
}

const menu=(state=initialState,action)=>{

    switch(action.payload){
        default:
            return  state;
    }


}


export default menu;